# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'momo_client' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'GoogleWebRTC'
  pod 'Starscream'
  pod 'SwiftyJSON'

  target 'momo_clientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'momo_clientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
